#store only the even numbers between 1 and 10
#by making the rangefunction returning only every 2nd number
#the 3rd argument in the rangefuction is the stepsize
even_numbers = list(range(2, 11, 2))
print(even_numbers)